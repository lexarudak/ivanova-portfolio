export enum PAGES {
  home = "/",
  works = "/works",
  project = "/works/:projectId",
  about = "/about",
  contacts = "/contacts",
  architectWorks = "/works?filter=architect",
  designWorks = "/works?filter=design",
}
